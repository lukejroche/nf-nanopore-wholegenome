{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/ontwholegenome/master/assets/schema_input.json",
    "title": "nf-core/ontwholegenome pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "id": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "bam": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "errorMessage": "Bam file for sample must be provided as '.bam' file."
            },
            "reference": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "errorMessage": "Path to reference file for sample must be provided .fa or .fasta file"
            }
        },
        "required": ["id", "bam", "reference"]
    }
}
